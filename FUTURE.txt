Use Gemini for parsing the rubric if is an image being submitted specifically (such an edge case lol).

Test different edge cases for the marking criteria to fix it 

Also could test different models? 




Parsing Marking Criteria - Uses docling currently, if we're not happy with it we could consider Marker, use AI vision to parse it, or other alternatives. 

can consider Drag-and-Drop: dnd kit (Frontend)  down the line to 'move around' the criteria cells. 